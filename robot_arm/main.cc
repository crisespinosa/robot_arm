/**
 * @file main.cc
 * @brief Точка входа HTTP-сервера на базе Drogon для управления роботом-манипулятором.
 *
 * Запускает веб-сервер на порту 8848, который предоставляет REST API
 * для планирования траекторий, управления движением (LQR/MPC-lite)
 * и взаимодействия с PPO-агентом обучения с подкреплением.
 */

#include <drogon/drogon.h>
#include "controllers/ArmController.h"

int main() {
    // Слушаем на всех интерфейсах (0.0.0.0), порт 8848
    drogon::app().addListener("0.0.0.0", 8848);

    // Запуск основного цикла событий Drogon (блокирующий вызов)
    drogon::app().run();

    return 0;
}
